name: Calling reusable Workflows
on: 
    workflow_dispatch:
        inputs:
            name: 
                description: "Input description"
                type: string
                default: "Ali"
                required: false

jobs:
    calling-a-reusable-wf:
        permissions: 
            contents: read
        uses: GauriAgarwal0404/Github_Actions_Notes/.github/workflows/reusable.yaml@main
        with:
            name: ${{ inputs.name }}
        secrets:
            access-token: ${{ secrets.ACCESS_TOKEN }}



